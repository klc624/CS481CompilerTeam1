func main()
{
  var int a
  var int b
  var int c
  var bool d
  var bool e
  var bool f

  d = (e || T)
  d = ( 1 > 2 )
  d = ( 1 == 2 )
  d = ( T == F )
  d = ( a > c || ( e || f ))
  d = ( a > ( b + c ) && T )
  a = b * ( c + 4 ) / 7
}
