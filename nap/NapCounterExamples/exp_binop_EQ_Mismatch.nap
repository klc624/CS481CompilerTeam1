func main()
{
  var int a
  var int b
  var int c
  var bool d
  var bool e
  var bool f

  d = (e || T)
  d = ( 2 > 1 )
  d = ( 1 == F ) #Comparing disparate types
  d = ( T == F )
  d = ( a > c || ( e || f ))
  d = ( a > b + c && T )
}
